These various functions are used to create networks with different properties.

The easiest way to use them is to call "generateReservoirNetworkTopologies", and pass the parameters regarding network size, connectivity, topology and weight distributions